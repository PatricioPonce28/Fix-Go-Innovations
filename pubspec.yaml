name: fix_go_innovations
description: Fix&Go Innovations - Plataforma que conecta clientes con técnicos especializados

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Supabase
  supabase_flutter: ^2.5.6
  
  # Variables de entorno
  flutter_dotenv: ^5.1.0
  
  # 🔗 Deep Linking & Navigation
  go_router: ^13.0.0
  
  # Utilidades
  cupertino_icons: ^1.0.2

  # Cámara y permisos
  image_picker: ^1.0.7
  permission_handler: ^11.3.0

  intl: ^0.18.1

  flutter_map: ^6.1.0

  latlong2: ^0.9.0

  geolocator: ^10.1.0

  url_launcher: ^6.2.2

  # Pagos con Braintree
  braintree_flutter_plus: 5.1.1

  # 📬 Notificaciones
  flutter_local_notifications: ^17.0.0
  
  # 📳 Vibración (Haptic Feedback)
  vibration: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # ⚠️ IMPORTANTE: Incluir el archivo .env
  assets:
    - .env